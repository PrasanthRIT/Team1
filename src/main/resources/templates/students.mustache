<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>TigerBoard | Student-User</title>
    <link rel="stylesheet" href="/css/tigerboard-theme.css">
</head>
<body>

<header>
    <a href="/" class="logo-container">
        <img src="/logo.jpg" alt="TigerBoard Logo" class="logo-img" style="height: 50px;">
        <p style="color: black; font-size: 26px; font-weight: bold">TigerBoard</p>
    </a>
</header>

<main>
    <div class="section-header">
        <a href="/" class="btn btn-outline">Back to Home</a>
        <h2>Students List</h2>
        <a href="/students/add" class="btn btn-primary">Add New Student</a>
    </div>

    <div class="table-card">
        <div class="table-responsive">
            <table>
                <thead>
                <tr>
                    <th>ID</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Commute Plan</th>
                    <th>Location</th>
                    <th>Bus Assigned</th>
                </tr>
                </thead>
                <tbody>
                {{#StdList}}
                    <tr>
                        <td>{{user.id}}</td>
                        <td>{{user.firstName}}</td>
                        <td>{{user.lastName}}</td>
                        <td>{{commutePlan}}</td>
                        <td>{{location}}</td>
                        <td>{{assignedBus}}</td>
                    </tr>
                {{/StdList}}
                </tbody>
            </table>
        </div>
    </div>
</main>

</body>
</html>